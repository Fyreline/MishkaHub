# reference/

Drop initial reference material here — e.g. the Letterboxd CSV export ZIPs
(`example_user1.zip`, `example_user2.zip`), screenshots, or notes used while building.

Contents are gitignored (they contain personal data); only this README is tracked.
